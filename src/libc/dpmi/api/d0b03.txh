@node __dpmi_reset_debug_watchpoint, dpmi
@findex __dpmi_reset_debug_watchpoint
@subheading Syntax

@example
#include <dpmi.h>

int __dpmi_reset_debug_watchpoint(unsigned long _handle);
@end example

@subheading Description

Please refer to the DPMI Specification (@pxref{DPMI Specification})
for details on DPMI function call operation.  Also see
the DPMI Overview (@pxref{DPMI Overview}) for general information.

DPMI function AX = 0x0b03

Resets a watchpoint given its handle in @var{_handle}.

@subheading Return Value

-1 on error, else zero.

@subheading Portability

@portability !ansi, !posix

