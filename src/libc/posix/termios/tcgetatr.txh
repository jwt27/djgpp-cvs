@node tcgetattr, termios
@subheading Syntax

@example
#include <termios.h>

int tcgetattr (int fd, struct termios *termiosp);
@end example

@subheading Description

This function gets termios structure from device @var{fd} and stores
it in the structure @var{termiosp}.  Note that the termios emulation
handles console only.

@subheading Return Value

Zero on success, nonzero on failure.

@subheading Example

@example
struct termios termiosbuf;
int rc = tcgetattr (0, &termiosbuf);
@end example


