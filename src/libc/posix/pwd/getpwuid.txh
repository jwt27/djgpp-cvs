@node getpwuid, unix
@subheading Syntax

@example
#include <pwd.h>

struct passwd *getpwuid(uid_t uid);
@end example

@subheading Description

This function gets the password file entry matching @var{uid}.  @xref{getpwent}.

@subheading Return Value

The matching record, or @code{NULL} if none match.

