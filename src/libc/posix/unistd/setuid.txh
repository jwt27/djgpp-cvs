@node setuid, unix
@findex setuid
@subheading Syntax

@example
#include <unistd.h>

int setuid(uid_t uid);
@end example

@subheading Description

This function is simulated, since MS-DOS does not support user IDs.

@subheading Return Value

If @var{uid} is equal to that returned by @pxref{getuid}, returns zero.
Otherwise, returns -1 and sets @code{errno} to EPERM.


@subheading Portability

@portability !ansi, posix

