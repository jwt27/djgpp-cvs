@node _go32_dpmi_remaining_physical_memory, dpmi
@findex _go32_dpmi_remaining_physical_memory
@subheading Syntax

@example
#include <dpmi.h>

unsigned long _go32_dpmi_remaining_physical_memory(void);
@end example

@subheading Description

Returns the amount of physical memory that is still available in the
system. 

@subheading Return Value

The amount in bytes.

@subheading Portability

@portability !ansi, !posix

@c ----------------------------------------------------------------------
@node _go32_dpmi_remaining_virtual_memory, dpmi
@findex _go32_dpmi_remaining_virtual_memory
@subheading Syntax

@example
#include <dpmi.h>

unsigned long _go32_dpmi_remaining_virtual_memory(void);
@end example

@subheading Description

Returns the amount of virtual memory that is still available in the
system. 

@subheading Return Value

The amount in bytes.


@subheading Portability

@portability !ansi, !posix

